import type { NextPage } from "next";
import MainContainer from "../src/components/pageComponents/main/MainContainer";

const Home: NextPage = () => {
  return <MainContainer />;
};

export default Home;
